// ============================================================
// TEMPORARY product label for the public marketing site.
//
// The official product name is not finalized (see AGENTS.md / BUILD_STATE.md
// — "Product official name is still not finalized"). This uses the label
// already present in the repo (`package.json` name, current
// `<title>` in `src/app/layout.tsx`) rather than inventing or locking in a
// permanent brand — renaming later is a one-line change here, not a
// find-and-replace across every marketing component.
// ============================================================

export const PRODUCT_NAME = 'wacrm'
